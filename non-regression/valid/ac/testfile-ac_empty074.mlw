

logic a,b,c,d,e,gamma,beta,x,y:int
logic ac f,g : int,int -> int

goal g74 :
  f(a,b) = gamma ->
  f(a,c) = beta ->
  (* f(gamma,c) = f(beta,b) est déduit *)
  g(f(f(a,c),b),d) = x ->
  g(f(beta,b),e) = y ->
  g(x,e) = g(y,d)

