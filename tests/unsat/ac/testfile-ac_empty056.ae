

logic a,b,c,x,gamma,beta,delta,omega : int
logic ac f,g:int,int -> int

goal g56 :
  f(a,b) = gamma ->
  f(a,c) = beta ->
  g(x,gamma)= delta ->
  g(x,beta) = omega ->
  g(beta,delta) = g(gamma,omega)

