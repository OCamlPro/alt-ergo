(set-logic ALL)
(set-option :produce-models true)
(declare-datatype Pair
((pair (first Int) (second Int))))
(declare-fun f (Int) Pair)
(declare-const x Int)
(declare-const y Int)
(assert (= (first (f x)) 5))
(assert (= (first (f 0)) y))
(assert (= x 0))
(check-sat)
(get-model)
