(set-option :produce-models true)
(set-option :produce-assignments true)
(set-logic ALL)
(declare-const x Int)
(declare-fun f (Int) Int)
(assert (! (> (f x) 0) :named foo))
(assert (< (f 0) 0))
(check-sat)
(get-value (x (+ x 5)))
(get-assignment)
