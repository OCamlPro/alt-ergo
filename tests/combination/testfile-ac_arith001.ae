
logic a,b,c,x,beta,gamma,d:int

logic ac f : int,int -> int

goal g1 : 
     f((f(a,b) + x),c) = gamma -> 
     x = 1 -> 
     f(a,c) = a -> 
     f(a,d) = beta -> 
     f(b,beta) = f(d,gamma)
     
